async function transcribeAudio(audioPath) {
  // TODO: Implement Whisper transcription logic.
  return { transcript: `Transcription placeholder for ${audioPath}` };
}

module.exports = { transcribeAudio };
