/** Phase-one shell. Queue controls are implemented in subsequent phases. */
export function App() {
  return <main className="popup"><p className="eyebrow">OPEN SOURCE WORKFLOW MANAGER</p><h1>VIDS AUTOMATION</h1><p className="status">Project scaffold ready. The queue engine and provider adapters are not enabled yet.</p></main>;
}
